# 2026-07-13

## 오늘 한 일

- 지난 세션(2026-07-12)에서 USB 연결이 끊겨 미확인 상태로 남았던 릴리즈 빌드 실행 확인을 재시도
  - `JAVA_HOME`/`ANDROID_HOME` 재설정(새 Bash 셸), `adb devices -l`로 기기(`R3KL20GA0EE`, Galaxy S24) 연결 확인
  - 빌드 전 `adb shell input keyevent KEYCODE_WAKEUP`으로 화면부터 깨워 이전 세션의 "화면 잠금으로 마지막 실행 단계 멈춤" 패턴(`docs/troubleshooting/adb-install-hang-locked-device.md`) 예방
  - `npx expo run:android --variant release` 백그라운드 실행 → `BUILD SUCCESSFUL in 3m 30s`
  - "설치 완료"와 "앱 실행"을 CLI 로그가 아니라 기기 아티팩트로 각각 확인: `dumpsys package`의 `lastUpdateTime=2026-07-13 02:06:42`, `dumpsys activity activities`의 `ResumedActivity=com.anonymous.nightcap/.MainActivity`
  - `docs/troubleshooting/release-build-launch-confirmation-cut-off.md`에 해결 결과 반영, CLAUDE.md 알려진 제약 항목 갱신(미해결 → 해결)

## 다음에 할 일

- 이번 확인은 "앱이 켜진다"만 검증된 것 — PROJECT.md §7 체크리스트(공유시트 이미지/URL 유입, 온보딩 3장, 스캔/DRM/휴지통 등)는 여전히 실기기 수동 검증 필요
