# 2026-07-07

## 오늘 한 일

- W3-1 캡처 파이프라인 구현: `expo-media-library` 권한 요청(세분화 `photo` 권한, `accessPrivileges` 부분접근 판별) → `Screenshots` 앨범 우선 + 파일명 폴백 스캔 → 앱 샌드박스 사본 복사(`expo-file-system` `File`/`Directory` 클래스) → `react-native-image-colors` 평균 휘도로 DRM 분기 → `captures` INSERT (`src/services/screenshotScan.ts`)
- 스캔을 `initDb`(첫 렌더를 막는 Suspense 경로)에서 분리, `TriageScreen` 마운트 + `AppState` 포그라운드 시점에 비동기 실행. 결과는 `loadStack()` 전체 교체가 아니라 큐 꼬리에 append-only 병합, 드래그/별점모드 진행 중이면 병합을 지연
- SQLite 마이그레이션 2 추가: `meta`(스캔 워터마크) 테이블 + `asset_id` UNIQUE 인덱스(`INSERT OR IGNORE` 중복 방지)
- 카드/보관함/휴지통/별점모드 미니프리뷰에 실이미지 렌더링(`CoverImage` top-crop 컴포넌트 신규), DRM 카드는 제목 `TextInput` 직접 입력으로 교체
- 권한 거부 화면(`MediaAccessDeniedScreen`), 부분허용 배너(`PartialAccessBanner`) 신규
- 목데이터 시딩을 `MOCK_SEED_ENABLED = __DEV__ && false` 명시적 스위치 뒤로 이동(기본 비활성)
- 연결된 실기기(Galaxy S24+, R3KL20GA0EE)에 릴리즈 빌드 진행 — `react-native-image-colors`가 AGP8+ 환경에서 Kotlin/Java JVM 타깃 불일치로 컴파일 실패하는 문제를 로컬 config plugin(`app/plugins/withKotlinJvmTargetFix.js`)으로 해결, 이후 빌드 성공 + 기기 설치 확인

## 다음에 할 일

- PROJECT.md W3-1 §7 체크리스트를 실기기에서 직접 검증(스샷 스캔/삭제 다이얼로그/휴지통 복원/DRM 분기/부분권한/재실행 중복방지)
- `DRM_LUMINANCE_THRESHOLD` 실기기 튜닝 — 다크모드 앱(카톡/유튜브) 오탐 여부 확인
- 공유 카드 화면, 온보딩(백탭/버블), Android 플로팅 버블 네이티브 모듈 착수

## 메모

- 릴리즈 빌드는 "성공"했지만 이건 컴파일+설치까지만 확인된 것 — 실제 스캔/DRM/휴지통 플로우는 아직 실기기 수동 검증 전. 다음 세션에서 "빌드 성공 = 기능 동작"으로 착각하지 말 것
- `expo prebuild`는 매 실행마다 `android/`를 완전히 지우고 재생성하므로, 네이티브 관련 커스텀(이번엔 JVM 타깃 fix)은 반드시 config plugin으로 넣어야 다음 prebuild에서 유실되지 않는다

---

## 추가 세션 (W3-2, 저녁)

### 오늘 한 일

- 도그푸딩 피드백 반영: 전 스크린샷 자동 유입이 업무/일회성 캡처까지 정리 대상으로 밀어넣어 "정리가 숙제가 되는" 문제 확인 → 유입 경로를 **공유시트 주 경로 + 자동 스캔 옵션(기본 OFF)** 구조로 전환(`docs/decisions/share-intent-primary-ingestion.md`)
- `expo-share-intent` 도입, `App.tsx`에서 항상 수신 → `services/shareIntake.ts`가 이미지(사본복사+DRM판별 재사용, `content_hash` dedup)/URL·텍스트(출처 앱 추론) 분기 INSERT
- 앱 기본 화면을 `TriageScreen`에서 `HomeScreen`(오늘 담은 개수 + "정리 시작")으로 분리, `TriageScreen`은 명시적 진입/종료(`onExit`)하는 화면으로 전환
- 보관함 상세 화면(`LibraryDetailScreen`) 신설: 원본 이미지, 별점 수정(`RateModeLayer` 재사용 + `onBackgroundTap`으로 실수 덮어쓰기 방지), 삭제
- 설정 화면 신설(스크린샷 자동 수집 토글 1개)
- Android edge-to-edge 하단 잘림 수정: 전 화면 `SafeAreaView edges={['top']}` + `useSafeAreaInsets().bottom`을 하단 고정 요소에 명시적으로 더하는 방식으로 통일
- 3개 논리 커밋으로 분리(하단 인셋 fix / 보관함 상세 / 공유시트 전환) — 검증 실패 시 마지막 커밋만 되돌릴 수 있게
- dev-client 디버그 빌드(`expo prebuild` + `gradlew assembleDebug`) 후 `adb install`, `aapt dump xmltree`로 최종 APK에 `SEND` 인텐트필터(`image/*`, `text/*`)가 정상 등록됐는지 확인. `expo run:android`는 빌드 후 자동으로 `am start`까지 실행해버려서 안 쓰고, gradle 직접 호출 + 수동 `adb install`로 설치까지만 진행

### 다음에 할 일

- 실기기에서 공유시트 유입 검증(이미지/URL 공유, 자동수집 OFF 확인, 신규 설치 시 권한 프롬프트 안 뜨는지)
- iOS 공유시트(Share Extension) 실기기 검증 — macOS/Xcode 환경이 없어 이번 라운드는 Android만, iOS는 `expo prebuild`까지만 확인

### 메모

- `expo run:android`는 빌드+설치+실행(`am start`)을 한 번에 하는데, 화면이 꺼진 상태에서 마지막 단계가 대기만 하고 있는 걸 "멈췄다"고 오판했던 과거 사례(`docs/troubleshooting/adb-install-hang-locked-device.md`)가 있어 이번엔 아예 gradle을 직접 호출해 설치 단계에서 멈추는 방식으로 우회
