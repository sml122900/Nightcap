# 2026-07-07

## 오늘 한 일

- W3-1 캡처 파이프라인 구현: `expo-media-library` 권한 요청(세분화 `photo` 권한, `accessPrivileges` 부분접근 판별) → `Screenshots` 앨범 우선 + 파일명 폴백 스캔 → 앱 샌드박스 사본 복사(`expo-file-system` `File`/`Directory` 클래스) → `react-native-image-colors` 평균 휘도로 DRM 분기 → `captures` INSERT (`src/services/screenshotScan.ts`)
- 스캔을 `initDb`(첫 렌더를 막는 Suspense 경로)에서 분리, `TriageScreen` 마운트 + `AppState` 포그라운드 시점에 비동기 실행. 결과는 `loadStack()` 전체 교체가 아니라 큐 꼬리에 append-only 병합, 드래그/별점모드 진행 중이면 병합을 지연
- SQLite 마이그레이션 2 추가: `meta`(스캔 워터마크) 테이블 + `asset_id` UNIQUE 인덱스(`INSERT OR IGNORE` 중복 방지)
- 카드/보관함/휴지통/별점모드 미니프리뷰에 실이미지 렌더링(`CoverImage` top-crop 컴포넌트 신규), DRM 카드는 제목 `TextInput` 직접 입력으로 교체
- 권한 거부 화면(`MediaAccessDeniedScreen`), 부분허용 배너(`PartialAccessBanner`) 신규
- 목데이터 시딩을 `MOCK_SEED_ENABLED = __DEV__ && false` 명시적 스위치 뒤로 이동(기본 비활성)
- 연결된 실기기(Galaxy S24+, R3KL20GA0EE)에 릴리즈 빌드 진행 — `react-native-image-colors`가 AGP8+ 환경에서 Kotlin/Java JVM 타깃 불일치로 컴파일 실패하는 문제를 로컬 config plugin(`app/plugins/withKotlinJvmTargetFix.js`)으로 해결, 이후 빌드 성공 + 기기 설치 확인

## 다음에 할 일

- PROJECT.md W3-1 §7 체크리스트를 실기기에서 직접 검증(스샷 스캔/삭제 다이얼로그/휴지통 복원/DRM 분기/부분권한/재실행 중복방지)
- `DRM_LUMINANCE_THRESHOLD` 실기기 튜닝 — 다크모드 앱(카톡/유튜브) 오탐 여부 확인
- 공유 카드 화면, 온보딩(백탭/버블), Android 플로팅 버블 네이티브 모듈 착수

## 메모

- 릴리즈 빌드는 "성공"했지만 이건 컴파일+설치까지만 확인된 것 — 실제 스캔/DRM/휴지통 플로우는 아직 실기기 수동 검증 전. 다음 세션에서 "빌드 성공 = 기능 동작"으로 착각하지 말 것
- `expo prebuild`는 매 실행마다 `android/`를 완전히 지우고 재생성하므로, 네이티브 관련 커스텀(이번엔 JVM 타깃 fix)은 반드시 config plugin으로 넣어야 다음 prebuild에서 유실되지 않는다
