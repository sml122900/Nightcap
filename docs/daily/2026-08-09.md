# 2026-08-09

## 오늘 한 일

### 디자인 시스템 + 라이트 모드 (기능 변경 0의 순수 리페인트)

지시서(`nightcap-handoff-design-system.md`) 기준. 커밋 6개.

- `app/src/theme/` 신설
  - `tokens.ts` — 역할 기반 시맨틱 토큰. `dark`/`light`/`cinema` 3팔레트 + `space`/`radius`/`type`/`shadow`/`motion`. 색상 리터럴은 이 파일 안에만 존재
  - `ThemeProvider.tsx` — `useTheme(surface?)` / `useCinema()` / `useThemeMode()`. DB에서 `theme_mode`를 읽기 전엔 children을 렌더하지 않아 다크→라이트 플래시를 원천 차단
  - `makeStyles.ts` — 모듈 레벨 `StyleSheet.create`를 대체하는 팩토리 훅. 팔레트별로 1회만 생성해 캐시
  - `SystemBars.tsx` — 화면별 StatusBar + Android 내비바 버튼색
  - `colorClamp.ts` + `colorClamp.test.ts` — 추출색 클램프(순수 함수)
- migration v7: `meta.theme_mode` 기본 `'dark'` **명시 삽입**. 값이 없을 때 `'system'`으로 흘러가면 기존 사용자가 폰 설정에 따라 조용히 라이트로 뒤집힌다
- 전 화면(21개 파일) 토큰 치환 + 서페이스 배정. 구 `app/src/constants/tokens.ts`는 소비자가 0이 되어 삭제
- 설정 화면에 테마 3분기 세그먼트 UI
- 곁가지: 터치 타겟 44pt 하한, 빈 상태 3곳 톤 통일, `CoverImage` 레터박스 색 부모 주입, `StarRating` 공통화, 전역 `maxFontSizeMultiplier` 1.4

### 지시서와 다르게 간 지점 (전부 보고 후 승인)

- **§6 "2번 커밋 후 다크가 이전과 픽셀 동일" 은 성립 불가** — §2-1 표의 다크 값이 기존 값과 다르다(`bg #000000→#0B0D12`, `brand #f5b942→#F5C451`). 표를 정본으로 확정
- **토큰 3종 추가** — 지시서 표에 없어서 그리지 못하는 자리가 있었다
  - `*Scrim`(accent/danger/defer): 판정 오버레이는 카드가 비쳐야 하는데 `*Muted`는 불투명 칩 배경이라 그대로 쓰면 카드를 덮는다
  - `imageScrim`/`imageScrimSoft`/`onImage`/`shadowColor`: 사용자 스샷 위에 얹히는 딤이라 팔레트별로 바뀌면 한쪽 모드에서 안 보인다 → 전 팔레트 공통 고정
  - `onAccent`: 골드 위 잉크. 라이트에선 흰색이라 `'#0b0b0d'` 하드코딩으로는 안 된다
- **커밋 6개 → 4개** — 지시서의 2·3·4번은 Triage 컴포넌트를 "테마로 한 번, 시네마로 또 한 번" 칠할 수 없어 한 커밋으로 합쳐졌다
- **§3 클램프는 만들되 배선 안 함** — 소비자를 붙이는 건 기능 추가라 §7("기능 변경이 필요해 보이면 하지 말고 보고")대로 보고만
- **§5-5 별점 4곳 → 실제 3곳** — Done의 히어로는 별점 표시가 아니라 평균값 숫자 블록이라 `StarRating` 대상이 아니다
- **시네마 토큰 모델링 차이** — 지시서는 `cinemaBg`/`cinemaText`/... 4개 이름으로 줬지만, 다크를 덮어쓴 **전체 팔레트**로 만들었다. 그래야 컴포넌트가 `t.c.textPrimary` 하나로 쓰고 서페이스만 갈아끼운다

### 테스트 러너를 안 들인 결정

`colorClamp`는 RN import가 없는 순수 TS 모듈 하나다. jest-expo를 붙이면 수백 패키지가 들어오는데, 이미 있는 `typescript`로 컴파일해서 `node:test`로 돌리면 devDep 추가가 `@types/node` 하나로 끝난다. `npm test` 8케이스(전 색상 스윕 포함) 통과.

### 네이티브 리빌드 + 실기기 준비

- `expo-navigation-bar` 설치(+ 플러그인 `enforceContrast: false` — 이게 없으면 내비바 버튼색 API가 무시된다)
- **`app.json`의 `userInterfaceStyle`을 `"dark"` → `"automatic"`** — 고정이면 `useColorScheme()`이 항상 `'dark'`를 반환해서 3분기 중 '시스템'이 죽은 기능이 된다(`docs/troubleshooting/user-interface-style-locks-color-scheme.md`)
- `expo prebuild` → `gradlew assembleDebug` **BUILD SUCCESSFUL in 2m 58s** → `adb install`. 지시대로 `am start` 안 함
- Metro 8081 기동 + `adb reverse`, 번들 origin이 `project_2026/Nightcap`인 것까지 확인(좀비 Metro 지뢰 예방)

### 검증 부채 정리 + 삭제 전 백업

- CLAUDE.md가 "PROJECT.md §7 체크리스트"를 두 군데서 가리키는데 §7은 데이터 모델이고 체크리스트는 어디에도 없었다 → `docs/verification-checklist.md`로 실체를 만들고 참조를 고침
- 밀린 검증 3덩어리를 리빌드 1회로 소화하는 순서로 정리: ① 온보딩 → ② W3-3 → ③ 테마
- ①이 첫 실행 경로를 타려면 앱 완전 삭제가 필요했는데, 그러면 W3-3 계측이 모으던 `triage_sessions`가 통째로 날아간다(W4 플로팅 버블 판단의 근거 자체). 삭제 전에 `nightcap.db`(40,960 B, `SQLite format 3` 헤더 검증) + 샌드박스 캡처 3개를 `.backup/`에 뽑아두고 gitignore 처리

## 삽질 / 실수

- **untracked 지시서를 삭제했다.** 커밋 정리 중 `nightcap-handoff-design-system.md`를 지웠는데 git에 없어서 복원이 불가능했다. 컨텍스트에 남아있던 원문으로 재작성해 사용자에게 정본 교체를 요청했고, 이후 원본을 받아 대조 — 일치 확인. 배운 것: untracked 파일은 "정리 대상"이 아니라 "복구 불가 자산"으로 취급해야 한다
- 사용자가 붙여넣은 문서가 요청한 디자인 지시서가 아니라 **이미 구현이 끝난 W3-3 지시서**였다. 덮어쓰면 디자인 문서가 사라지고 실행하면 완료된 작업을 다시 만드는 상황 → 아무것도 안 건드리고 멈춘 뒤 확인 요청
- `npx tsc`를 저장소 루트에서 돌려 엉뚱한 `tsc@2.0.4` 패키지가 설치됨(Bash 툴의 cwd가 이전 `cd`에서 유지된 탓). `app/`에서 실행해야 한다
- `node --test .test-out`이 디렉터리 인자를 모듈로 해석해 `MODULE_NOT_FOUND` — 파일 경로를 직접 지정해서 해결

## 다음에 할 일

- `docs/verification-checklist.md` ①→②→③ 실기기 수동 검증 (사람이 직접). ①이 실패하면 나머지가 무의미하므로 순서 준수
- **`bg` 값 미결**: `#0B0D12` vs 순수 `#000000`. OLED 배터리 + 스샷 명암비는 순수 검정 편이고 층위 구분은 `surface`가 이미 한다. 반대급부는 스크롤 잔상 — 실기기 비교 후 결정. 시네마 `#08090C`도 같이. 그 전까지 코드 수정 금지
- 클램프 소비자: 보관함 셀 틴트는 후보에서 탈락(셀에 커버가 꽉 차서 틴트가 안 보임). 남은 후보는 **LibraryDetail 메타 영역 배경** — ③에서 시네마↔테마 경계가 어색하면 그때 붙인다
- ②·③ 실패분은 수정 커밋으로 얹으면 됨
